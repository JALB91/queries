export default class utils {
    getBBoxSection(sLat, sLon, eLat, eLon, scaleFactor = 0.5) {
        const distanceLat = Math.abs(eLat - sLat);
        const distanceLon = Math.abs(eLon - sLon);
        
    }
}